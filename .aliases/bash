shopt -s extglob

for i in ~/.aliases/*.alias; do source $i; done

complete -cf sudo
for i in ~/.bash_completion.d/*; do source "$i"; done

cnf_hook=/usr/share/doc/pkgfile/command-not-found.bash
[ -f $cnf_hook ] && . $cnf_hook

# vim:ft=sh
